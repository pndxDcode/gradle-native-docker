<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.refactory.app.refactoryapps">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:host="app.refactory.id"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:pathPrefix="/oauth/authorize"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:scheme="https"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GitLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Hapus"
            android:label="@string/title_activity_hapus"
            android:theme="@style/AppTheme.NoActionBar" />
        <!-- <activity android:name=".GitLogin"></activity> -->
        <activity android:name=".Dashboard" />
    </application>

</manifest>